<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
       <a href="{{ path('etudiant_index') }}">Etudiant  |</a>
       <a href="{{ path('professeur_index') }}">Professeur  |</a>
       <a href="{{ path('classe_index') }}">Classe  |</a>
       <a href="{{ path('salle_index') }}">Salle  |</a>
       <a href="{{ path('matiere_index') }}">Matiere  |</a>
       <a href="{{ path('seance_index') }}">Seance  |</a>
        {% block body %}
        {% endblock %}
    </body>
</html>
