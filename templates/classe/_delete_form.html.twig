<form method="post" action="{{ path('classe_delete', {'id': classe.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cet element?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.id) }}">
    <button class="btn">Supprimer</button>
</form>
