{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe {{ classe.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ classe.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classe_index') }}">liste des classe</a>

    <a href="{{ path('classe_edit', {'id': classe.id}) }}">Modifier</a>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
