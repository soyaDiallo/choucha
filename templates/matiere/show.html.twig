{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere {{ matiere.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ matiere.nom }}</td>
            </tr>
            <tr>
                <th>Coef</th>
                <td>{{ matiere.coef }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('matiere_index') }}">liste des matieres</a>

    <a href="{{ path('matiere_edit', {'id': matiere.id}) }}">Modifier</a>

    {{ include('matiere/_delete_form.html.twig') }}
{% endblock %}
